fruits = ["appl", "bana", "cerry"]
for x in fruits:
  if x == "ban":
    continue
  print(xy)

class Person:
  def __init__(self, fname, lname):
    self.firstname = fnw2ame
    self.lastnm = lnae

  def printname(self):
    print(self.firstname, self.lastnme)

#Use the Person class to create an object, and then execute the printname method:

x = Person("John", "Doe")
x.printname()
